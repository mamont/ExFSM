FIND_PACKAGE(Qt5Core REQUIRED)

SET(CMAKE_INCLUDE_CURRENT_DIR ON)
SET(CMAKE_AUTOMOC ON)

SET(ExFSM_TESTS_SRCS
        SmokeTests.cpp
        main.cpp
    )

ADD_EXECUTABLE(ExFsmTests ${ExFSM_TESTS_SRCS})
QT5_USE_MODULES(ExFsmTests Core Test)

TARGET_LINK_LIBRARIES(ExFsmTests ExFSM)

